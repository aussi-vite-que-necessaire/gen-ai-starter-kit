name: Deploy App

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # 1. FIX MINUSCULE
      - name: Lowercase Repo Name
        run: |
          echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 2. BUILD API
      - name: Build and Push API
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./apps/api/Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO_LOWER }}/api:latest

      # 3. BUILD WEB
      - name: Create Web Env for Production
        run: |
          echo "VITE_API_URL=https://api.${{ vars.MAIN_DOMAIN }}" > ./apps/web/.env

      - name: Build and Push Web
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./apps/web/Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO_LOWER }}/web:latest

      # 4. DEPLOY TO VPS
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Nettoyage prudent (uniquement images, pas les volumes !)
            echo "🧹 Ménage pré-déploiement..."
            docker system prune -a -f 

            mkdir -p ~/apps/${{ vars.PROJECT_NAME }}
            cd ~/apps/${{ vars.PROJECT_NAME }}

            # Récupération de la variable minuscule
            REPO_LOWER="${{ env.REPO_LOWER }}"

            # --- FICHIERS ---
            mkdir -p apps/automation/seed
            mkdir -p apps/automation/custom-node

            # 1. Docker Compose
            curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -o docker-compose.yml \
                 -L https://raw.githubusercontent.com/${{ github.repository }}/main/docker-compose.yml

            # 2. Script création DB (dans le sous-dossier)
            curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -o apps/automation/seed/create-databases.sql \
                 -L https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/seed/create-databases.sql

            # 3. Seed n8n (A la RACINE pour simplifier le 'docker cp')
            curl -f -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -o init-n8n.sql \
                 -L https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/seed/init-n8n.sql

            # 4. Custom n8n nodes
            echo "📦 Téléchargement custom nodes..."
            curl -sf -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -o apps/automation/custom-node/package.json \
                 -L https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/custom-node/package.json

            # Télécharge le dossier dist (fichiers compilés)
            mkdir -p apps/automation/custom-node/dist/nodes/generated
            for file in index.js; do
              curl -sf -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                   -o "apps/automation/custom-node/dist/${file}" \
                   -L "https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/custom-node/dist/${file}" || true
            done
            for file in GenAiUpdateStatus.node.js GenAiPageGenerationTrigger.node.js GenAiPageGenerationComplete.node.js; do
              curl -sf -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                   -o "apps/automation/custom-node/dist/nodes/generated/${file}" \
                   -L "https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/custom-node/dist/nodes/generated/${file}" || true
            done

            # --- ENV ---
            echo "Génération du fichier .env..."
            cat <<EOF > .env
            # --- DOCKER ---
            IMAGE_NAME_API=ghcr.io/$REPO_LOWER/api:latest
            IMAGE_NAME_WEB=ghcr.io/$REPO_LOWER/web:latest
            PROJECT_NAME=${{ vars.PROJECT_NAME }}
            ENV_TYPE=production

            # --- TRAEFIK ---
            DOMAIN_NAME_WEB=${{ vars.MAIN_DOMAIN }}
            DOMAIN_NAME_API=api.${{ vars.MAIN_DOMAIN }}
            DOMAIN_NAME_N8N=n8n.${{ vars.MAIN_DOMAIN }}

            # --- SECRETS & N8N ---
            N8N_URL=https://n8n.${{ vars.MAIN_DOMAIN }}
            INTERNAL_API_SECRET=${{ secrets.INTERNAL_API_SECRET }}
            N8N_ENCRYPTION_KEY=${{ secrets.N8N_ENCRYPTION_KEY }}
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}

            # --- API CONFIG ---
            NODE_ENV=production
            PORT=3000
            FRONTEND_URL=https://${{ vars.MAIN_DOMAIN }}

            # --- DB & REDIS ---
            DATABASE_URL=postgresql://app_user:${{ secrets.DB_PASSWORD }}@db:5432/webapp
            REDIS_HOST=redis
            REDIS_PORT=6379

            # --- AUTH ---
            BETTER_AUTH_SECRET=${{ secrets.BETTER_AUTH_SECRET }}
            BETTER_AUTH_URL=https://api.${{ vars.MAIN_DOMAIN }}

            # --- INFRA ---
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            POSTGRES_USER=app_user
            POSTGRES_DB=webapp
            EOF

            chmod 600 .env

            # --- SÉQUENCE DE DÉMARRAGE (PROD SAFE) ---

            echo "📥 Pulling images..."
            docker compose pull

            # PAS DE RESET DB ICI (PROD) !

            echo "🚀 Démarrage DB & Redis..."
            docker compose up -d db redis

            echo "⏳ Attente disponibilité DB..."
            until docker compose exec -T db pg_isready -U app_user -d webapp; do sleep 2; done

            echo "🛠 Création base n8n (si inexistante)..."
            docker compose exec -T db psql -U app_user -d webapp -c "CREATE DATABASE n8n;" || echo "Database n8n already exists"

            echo "💉 Vérification Seed Admin n8n..."
            # On vérifie si la table user existe déjà dans n8n. Si oui, on ne touche à rien pour ne pas écraser la prod.
            if ! docker compose exec -T db psql -U app_user -d n8n -c 'SELECT 1 FROM "user" LIMIT 1;' > /dev/null 2>&1; then
                echo "📭 Base n8n vide. Injection du Seed..."
                docker compose cp init-n8n.sql db:/tmp/init-n8n.sql
                docker compose exec -T db psql -U app_user -d n8n -f /tmp/init-n8n.sql
            else
                echo "✅ Base n8n déjà initialisée. Skip seed."
            fi

            echo "📦 Migrations API..."
            docker compose run --rm api npm run db:migrate || {
              echo "❌ ERREUR MIGRATIONS"
              exit 1
            }

            echo "🚀 Démarrage Final..."
            # --remove-orphans nettoie les vieux conteneurs si tu as changé le docker-compose
            docker compose up -d --remove-orphans

            echo "⏳ Attente démarrage n8n..."
            sleep 10

            echo "📥 Import des workflows et credentials n8n..."
            mkdir -p n8n-workflows n8n-credentials

            # Télécharge les workflows
            for file in CE1aB6M2zzy6mHq7 FdlYe3ZEhP2R6L66 gK3EwEQLmGjhUuy9 MegPrvZxisBRYhLw O59HjSzD4t2zNIRv qN0tbdDK5ltRLCI4 YJBSKUGzYtUKymEu; do
              curl -sf -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                   -o "n8n-workflows/${file}.json" \
                   -L "https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/workflows/${file}.json" || true
            done

            # Télécharge les credentials (dossier séparé)
            curl -sf -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -o "n8n-credentials/credentials.json" \
                 -L "https://raw.githubusercontent.com/${{ github.repository }}/main/apps/automation/workflows/credentials.json" || true

            # Copie dans n8n (dossiers séparés)
            docker compose exec -u node -T n8n mkdir -p /home/node/backups/workflows /home/node/backups/credentials
            docker compose cp n8n-workflows/. n8n:/home/node/backups/workflows/
            docker compose cp n8n-credentials/. n8n:/home/node/backups/credentials/

            # Import credentials puis workflows
            docker compose exec -u node -T n8n n8n import:credentials --input=/home/node/backups/credentials/credentials.json || echo "⚠️ Credentials import failed"
            docker compose exec -u node -T n8n n8n import:workflow --input=/home/node/backups/workflows/ || echo "⚠️ Workflow import failed"

            echo "✅ Déployé sur https://${{ vars.MAIN_DOMAIN }}"
