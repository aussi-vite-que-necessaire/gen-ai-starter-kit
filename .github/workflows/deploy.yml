name: Deploy App

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Lowercase Repo Name
        run: echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # --- 1. BUILD ---
      - name: Build API
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./apps/api/Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO_LOWER }}/api:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build Web
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./apps/web/Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO_LOWER }}/web:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build Custom N8N
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./apps/automation/Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO_LOWER }}/n8n-custom:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # --- 2. CONFIGURATION ---
      - name: Copy Config to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "docker-compose.yml,apps/automation/workflows/,apps/automation/seed/"
          target: "apps/${{ vars.PROJECT_NAME }}"

      # --- 3. DÉPLOIEMENT ---
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          # On passe DB_PASSWORD (le mot de passe admin)
          envs: DB_PASSWORD
          script: |
            export DB_PASSWORD=$DB_PASSWORD

            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker image prune -f

            # --- DEFINITION DES NOMS ---
            # On normalise les noms de bases de données
            PROJECT_DB_APP="${{ vars.PROJECT_NAME }}_app"
            PROJECT_DB_N8N="${{ vars.PROJECT_NAME }}_n8n"

            # --- OVERRIDE DATABASE_URL ---
            # On construit l'URL ici pour être sûr qu'elle pointe vers la bonne DB avec le compte Admin
            export DATABASE_URL="postgresql://admin_postgres:$DB_PASSWORD@postgres-central:5432/$PROJECT_DB_APP?schema=public"

            # --- PROVISIONING SQL ---
            echo "🐘 Provisioning Database on Central Postgres..."
            # On utilise admin_postgres pour créer les bases si elles manquent
            docker run --rm --network host \
              -e PGPASSWORD=$DB_PASSWORD \
              postgres:16-alpine \
              psql -h 127.0.0.1 -U admin_postgres -d postgres -c "
                /* Création DB App */
                SELECT 'CREATE DATABASE \"$PROJECT_DB_APP\" OWNER \"admin_postgres\"'
                WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = '$PROJECT_DB_APP')\gexec
                
                /* Création DB N8N */
                SELECT 'CREATE DATABASE \"$PROJECT_DB_N8N\" OWNER \"admin_postgres\"'
                WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = '$PROJECT_DB_N8N')\gexec
              "

            # --- SETUP ENV ---
            cd ~/apps/${{ vars.PROJECT_NAME }}
            REPO_LOWER="${{ env.REPO_LOWER }}"

            echo "🔧 Génération .env..."
            cat <<EOF > .env
            # Images
            IMAGE_NAME_API=ghcr.io/$REPO_LOWER/api:latest
            IMAGE_NAME_WEB=ghcr.io/$REPO_LOWER/web:latest
            IMAGE_NAME_N8N=ghcr.io/$REPO_LOWER/n8n-custom:latest

            # DB Config (On utilise l'Admin partout pour simplifier)
            POSTGRES_USER=admin_postgres
            DB_PASSWORD=$DB_PASSWORD

            # Noms dynamiques des bases
            POSTGRES_DB_APP=$PROJECT_DB_APP
            POSTGRES_DB_N8N=$PROJECT_DB_N8N

            # App Config
            PROJECT_NAME=${{ vars.PROJECT_NAME }}
            NODE_ENV=production
            PORT=3000

            # URLs
            DOMAIN_NAME_WEB=${{ vars.MAIN_DOMAIN }}
            DOMAIN_NAME_API=api.${{ vars.MAIN_DOMAIN }}
            DOMAIN_NAME_N8N=n8n.${{ vars.MAIN_DOMAIN }}
            FRONTEND_URL=https://${{ vars.MAIN_DOMAIN }}
            N8N_URL=https://n8n.${{ vars.MAIN_DOMAIN }}
            BETTER_AUTH_URL=https://api.${{ vars.MAIN_DOMAIN }}

            # Secrets
            DATABASE_URL=$DATABASE_URL
            INTERNAL_API_SECRET=${{ secrets.INTERNAL_API_SECRET }}
            N8N_ENCRYPTION_KEY=${{ secrets.N8N_ENCRYPTION_KEY }}
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
            BETTER_AUTH_SECRET=${{ secrets.BETTER_AUTH_SECRET }}

            # Redis
            REDIS_HOST=redis
            REDIS_PORT=6379
            EOF
            chmod 600 .env

            # --- START ---
            echo "📥 Pull & Start..."
            docker compose pull
            docker compose up -d --remove-orphans

            # --- SEED N8N ---
            echo "💉 Vérification Seed N8N..."
            docker run --rm --network web_network \
              -v $(pwd)/apps/automation/seed/init-n8n.sql:/tmp/init-n8n.sql \
              -e PGPASSWORD=$DB_PASSWORD \
              postgres:16-alpine \
              bash -c "
                # On teste si la table user existe
                if ! psql -h postgres-central -U admin_postgres -d $PROJECT_DB_N8N -c 'SELECT 1 FROM \"user\" LIMIT 1;' > /dev/null 2>&1; then
                   echo '📭 Base n8n vide. Injection du Seed...'
                   psql -h postgres-central -U admin_postgres -d $PROJECT_DB_N8N -f /tmp/init-n8n.sql
                else
                   echo '✅ Base n8n déjà initialisée.'
                fi
              "

            # --- MIGRATIONS API ---
            echo "📦 Migrations API..."
            # On passe explicitement la DATABASE_URL à la commande
            docker compose run --rm -e DATABASE_URL=$DATABASE_URL api npm run db:migrate

            # --- IMPORT WORKFLOWS ---
            echo "📥 Import N8N Workflows..."
            sleep 15
            WORKFLOWS_DIR="apps/automation/workflows"

            if [ -f "$WORKFLOWS_DIR/credentials.json" ]; then
              docker compose cp "$WORKFLOWS_DIR/credentials.json" n8n:/tmp/credentials.json
              docker compose exec -u node -T n8n n8n import:credentials --input=/tmp/credentials.json || echo "⚠️ Credentials import failed"
            fi

            for file in "$WORKFLOWS_DIR"/*.json; do
              filename=$(basename "$file")
              if [ "$filename" != "credentials.json" ]; then
                docker compose cp "$file" n8n:/tmp/workflow.json
                docker compose exec -u node -T n8n n8n import:workflow --input=/tmp/workflow.json || echo "⚠️ Workflow $filename import failed"
              fi
            done

            docker compose exec -u node -T n8n n8n update:workflow --all --active=true
            docker compose restart n8n

            echo "✅ Déploiement terminé !"
