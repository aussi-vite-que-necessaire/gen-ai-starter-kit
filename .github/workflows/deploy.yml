name: Deploy App

on:
  push:
    branches: ["main"]

jobs:
  # --- JOB 1 : CONSTRUCTION DES IMAGES DOCKER (PARALLÈLE) ---
  build-images:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [api, web]
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Lowercase Repo Name
        run: echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and Push ${{ matrix.service }}
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./apps/${{ matrix.service }}/Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO_LOWER }}/${{ matrix.service }}:latest
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  # --- JOB 2 : COMPILATION DU CUSTOM NODE (PARALLÈLE) ---
  # On le sort du deploy pour qu'il tourne PENDANT que les images se construisent
  build-n8n-node:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
          cache-dependency-path: apps/automation/custom-node/package-lock.json

      - name: Build Custom Node
        run: |
          cd apps/automation/custom-node
          npm ci
          npm run build
          # On nettoie pour ne garder que la prod
          rm -rf node_modules
          npm ci --omit=dev

      # On sauvegarde le résultat compilé pour le job suivant
      - name: Upload Custom Node Artifact
        uses: actions/upload-artifact@v4
        with:
          name: n8n-custom-node
          path: apps/automation/custom-node/
          retention-days: 1

  # --- JOB 3 : DÉPLOIEMENT (Le chef d'orchestre) ---
  deploy:
    needs: [build-images, build-n8n-node] # 👈 Attend que TOUT soit prêt
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Lowercase Repo Name
        run: echo "REPO_LOWER=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      # On récupère le custom node compilé par le job précédent
      - name: Download Custom Node Artifact
        uses: actions/download-artifact@v4
        with:
          name: n8n-custom-node
          path: apps/automation/custom-node

      # --- NETTOYAGE ---
      - name: Force Clean Remote Directory
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            mkdir -p apps/${{ vars.PROJECT_NAME }}
            cd apps/${{ vars.PROJECT_NAME }}
            docker run --rm -v "$(pwd):/clean" alpine rm -rf /clean/apps/automation

      # --- ENVOI ---
      - name: Copy Config to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "docker-compose.yml,apps/automation/"
          target: "apps/${{ vars.PROJECT_NAME }}"

      # --- SSH & SEED ---
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: DB_PASSWORD
          script: |
            export DB_PASSWORD=$DB_PASSWORD
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker image prune -f

            PROJECT_DB_APP="${{ vars.PROJECT_NAME }}_app"
            PROJECT_DB_N8N="${{ vars.PROJECT_NAME }}_n8n"
            export DATABASE_URL="postgresql://admin_postgres:$DB_PASSWORD@postgres-central:5432/$PROJECT_DB_APP?schema=public"

            cd ~/apps/${{ vars.PROJECT_NAME }}

            # --- A. BASES ---
            echo "🐘 Provisioning Database..."
            docker run --rm --network web_network \
              -e PGPASSWORD=$DB_PASSWORD \
              postgres:16-alpine \
              sh -c "
                createdb -h postgres-central -U admin_postgres $PROJECT_DB_APP || true
                createdb -h postgres-central -U admin_postgres $PROJECT_DB_N8N || true
              "

            # --- B. ENV ---
            echo "🔧 Génération .env..."
            REPO_LOWER="${{ env.REPO_LOWER }}"
            cat <<EOF > .env
            IMAGE_NAME_API=ghcr.io/$REPO_LOWER/api:latest
            IMAGE_NAME_WEB=ghcr.io/$REPO_LOWER/web:latest
            IMAGE_NAME_N8N=n8nio/n8n:latest 
            POSTGRES_USER=admin_postgres
            DB_PASSWORD=$DB_PASSWORD
            POSTGRES_DB_APP=$PROJECT_DB_APP
            POSTGRES_DB_N8N=$PROJECT_DB_N8N
            PROJECT_NAME=${{ vars.PROJECT_NAME }}
            NODE_ENV=production
            PORT=3000
            DOMAIN_NAME_WEB=${{ vars.MAIN_DOMAIN }}
            DOMAIN_NAME_API=api.${{ vars.MAIN_DOMAIN }}
            DOMAIN_NAME_N8N=n8n.${{ vars.MAIN_DOMAIN }}
            FRONTEND_URL=https://${{ vars.MAIN_DOMAIN }}
            N8N_URL=https://n8n.${{ vars.MAIN_DOMAIN }}
            BETTER_AUTH_URL=https://api.${{ vars.MAIN_DOMAIN }}
            DATABASE_URL=$DATABASE_URL
            INTERNAL_API_SECRET=${{ secrets.INTERNAL_API_SECRET }}
            N8N_ENCRYPTION_KEY=${{ secrets.N8N_ENCRYPTION_KEY }}
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
            BETTER_AUTH_SECRET=${{ secrets.BETTER_AUTH_SECRET }}
            REDIS_HOST=redis
            REDIS_PORT=6379
            EOF
            chmod 600 .env

            # --- C. START ---
            echo "📥 Pull & Start..."
            docker compose pull
            docker compose up -d --remove-orphans

            # --- D. SEED N8N (Try & Retry) ---
            echo "💉 Seed N8N..."
            sed -i 's/app_user/admin_postgres/g' apps/automation/seed/init-n8n.sql

            docker run --rm --network web_network \
              -v $(pwd)/apps/automation/seed/init-n8n.sql:/tmp/init-n8n.sql \
              -e PGPASSWORD=$DB_PASSWORD \
              postgres:16-alpine \
              bash -c "
                echo '⏳ Tentative d injection du Seed...'
                MAX_RETRIES=30
                COUNT=0
                SUCCESS=0
                while [ \$COUNT -lt \$MAX_RETRIES ]; do
                  if psql -h postgres-central -U admin_postgres -d $PROJECT_DB_N8N -f /tmp/init-n8n.sql > /dev/null 2>&1; then
                    echo '✅ Injection réussie !'
                    SUCCESS=1
                    break
                  fi
                  echo \"... N8N migre encore (Tentative \$((COUNT+1))/\$MAX_RETRIES)...\"
                  sleep 2
                  COUNT=\$((COUNT+1))
                done

                if [ \$SUCCESS -eq 0 ]; then
                  echo '❌ Échec Seed.'
                  psql -h postgres-central -U admin_postgres -d $PROJECT_DB_N8N -f /tmp/init-n8n.sql
                  exit 1
                fi
              "

            # --- E. POST-DEPLOY ---
            echo "📦 Migrations API..."
            docker compose run --rm -e DATABASE_URL=$DATABASE_URL api npm run db:migrate

            echo "📥 Import N8N Workflows..."
            WORKFLOWS_DIR="apps/automation/workflows"
            if [ -f "$WORKFLOWS_DIR/credentials.json" ]; then
              docker compose cp "$WORKFLOWS_DIR/credentials.json" n8n:/tmp/credentials.json
              docker compose exec -u node -T n8n n8n import:credentials --input=/tmp/credentials.json || true
            fi

            for file in "$WORKFLOWS_DIR"/*.json; do
              filename=$(basename "$file")
              if [ "$filename" != "credentials.json" ]; then
                docker compose cp "$file" n8n:/tmp/workflow.json
                docker compose exec -u node -T n8n n8n import:workflow --input=/tmp/workflow.json || true
              fi
            done

            docker compose exec -u node -T n8n n8n update:workflow --all --active=true
            docker compose restart n8n

            echo "✅ Déploiement terminé !"
